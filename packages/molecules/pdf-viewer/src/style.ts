export const style = `:host {
  display: block; }
  :host object,
  :host embed,
  :host iframe {
    width: 100%;
    border: 0; }
  :host pap-button[part="download-button"] {
    margin-top: var(--margin-small, 8px); }
  :host a.download {
    display: none; }`;
