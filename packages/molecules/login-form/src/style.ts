export const style = `:host o-typography[variant="t2"] {
  margin-bottom: var(--margin-medium); }

:host > div {
  display: flex;
  align-items: center;
  gap: var(--gap-small);
  margin-bottom: 3rem; }
  :host > div o-button {
    gap: 0;
    padding: 0; }

:host o-form o-button {
  margin-top: var(--gap-large); }`;
